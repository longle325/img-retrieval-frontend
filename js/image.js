const image_dropage = document.getElementById("input-file");

image_dropage.addEventListener("change", image_search);

function image_search(){
    image_dropage.files[0];
    let imgLink = URL.createObjectURL;
}